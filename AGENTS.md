# AGENTS.md

阅读 `resume_design_summary.md`，并参考 `examples/` 下的文件来生成简历。

必须保留 `examples/` 模板中的证件照区域。
- 基本信息区使用左右布局
- 左侧放个人信息
- 右侧保留证件照占位
- 不要删除 `avatar-container` 和 `avatar-placeholder` 结构
- 即使用户没有提供照片，也要保留证件照占位，方便后续手动替换

输出统一放在 `output/` 下：
- 用户生成的 `profile.md`
- 用户生成的 HTML
- 用户生成的 PDF
